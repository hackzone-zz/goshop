{{ template "header" . }}

	<!-- Main component for a primary marketing message or call to action -->
	<div class="jumbotron">
		<h1>{{ .Title }}</h1>
		<p>{{ .Description }}</p>
	</div>

{{ template "footer" . }}
